\subsubsection{One MPI rank per node}
\begin{figure}[H]
    \centering
    \incfig[1]{gflops\_2x4\_multi\_defq} 
    \caption{Sustained GFLOPS performance of SpMV over 100 iterations on 1–4 nodes (one MPI rank per node) using dual-socket \defq{} processors.}
    \label{fig:gflopsdefqmulti}
\end{figure}
\medskip

\begin{figure}[H]
    \centering
    \incfig[1]{t\_2x4\_multi\_defq} 
    \caption{Total execution time per iteration of SpMV on 1–4 nodes (one MPI rank per node) using dual-socket \defq{} processors.}
    \label{fig:tdefqmulti}
\end{figure}


The results in Figure \ref{fig:gflopsdefqmulti} and \ref{fig:tdefqmulti} illustrate the sustained GFLOPS performance, and total execution time per iteration SpMV. This configuration uses one MPI rank per node. As is evident from the plots, using only one node (and one MPI rank), consistently yields results that are either better or almost as good as the results achieved when using the maximum amount of available nodes, and when compared to using 64 MPI ranks, one per physical core, the performance is much worse. As an example, for the \texttt{Bump\_2911} matrix, Strategy D achieves a GFLOPS performance of \(\approx 9\) GFLOPS on one node, with 64 OpenMP shared memory threads. When this is compared to Figure \ref{fig:gflopsdefqsingle} using 64 MPI ranks, which achieves \(\approx 30\) GFLOPS for the same matrix.
\medskip

From these results, it is clear that as soon as the network cards connecting the nodes are accessed, performance sharply drops. The exact reason for this is outside the scope of this thesis. Thune et. al \cite{10064025} performed a study investigating point-to-point MPI communication, and their findings yielded similar degradations in performance when moving to inter-node communications.

\begin{figure}[H]
    \centering
    \incfig[1]{tcomp\_2x4\_multi\_defq} 
    \caption{Computation time per iteration of SpMV on 1–4 nodes (one MPI rank per node) using dual-socket \defq{} processors.}
    \label{fig:tcompdefqmulti}
\end{figure}

\begin{figure}[H]
    \centering
    \incfig[1]{tcomm\_2x4\_multi\_defq} 
    \caption{Communication time per iteration of SpMV on 1–4 nodes (one MPI rank per node) using dual-socket \defq{} processors.}
    \label{fig:tcommdefqmulti}
\end{figure}

Figure \ref{fig:tcompdefqmulti} and \ref{fig:tcommdefqmulti} illustrate the computation and communication time respectively per iteration of SpMV. For the first plot, nothing out of the ordinary is observed, except for some erraticism on some of the matrices. For the second plot, however, we can see that the communication time of Strategy E starts off significantly higher than the other strategies, before sharply dropping as the number of ranks increases. We can observe the start of some trends, with both Strategy B and C increasing in communication time, Strategy D remaining relatively stable, and Strategy E approaching that of Strategy D. The reasons for the worse performance of Strategy E will be discussed in Section \ref{sec:romemulti}.

\begin{figure}[H]
    \centering
    \incfig{commload\_2x4\_multi\_defq} 
    \caption{Fraction of the global \(x\) vector communicated per iteration of SpMV on 1–4 nodes (one MPI rank per node) using dual-socket \defq{} processors.}
    \label{fig:commloaddefqmulti}
\end{figure}

\subsubsection{One MPI rank per socket}

\begin{figure}[H]
    \centering
    \incfig[1]{gflops\_2x4\_multi\_defqmpi}
    \caption{Sustained GFLOPS performance of SpMV over 100 iterations on 1–4 nodes (one MPI rank per socket) using dual-socket \defq{} processors.}
    \label{fig:gflopsdefqmpimulti}
\end{figure}

\begin{figure}[H]
    \centering
    \incfig[1]{t\_2x4\_multi\_defqmpi}
    \caption{Total execution time per iteration of SpMV on 1–4 nodes (one MPI rank per socket) using dual-socket \defq{} processors.}
    \label{fig:tdefqmpimulti}
\end{figure}


Figure \ref{fig:gflopsdefqmpimulti} and \ref{fig:tdefqmpimulti} illustrate the sustained GFLOPS performance achieved across 100 iterations of SpMV, and the execution time per iteration of Spmv, respectively. When compared to the results in Figure \ref{fig:gflopsdefqmulti} and \ref{fig:tdefqmulti}, we see that the performance is better in the multi-node case by \(\approx\) 1-2 GFLOPS in most matrices. We also observe a greater spread in performance across the strategies for all configurations.

\begin{figure}[H]
    \centering
    \incfig[1]{tcomp\_2x4\_multi\_defqmpi}
    \caption{Computation time per iteration of SpMV on 1–4 nodes (one MPI rank per socket) using dual-socket \defq{} processors.}
    \label{fig:tcompdefqmpimulti}
\end{figure}

\begin{figure}[H]
    \centering
    \incfig[1]{tcomm\_2x4\_multi\_defqmpi}
    \caption{Communication time per iteration of SpMV on 1–4 nodes (one MPI rank per socket) using dual-socket \defq{} processors.}
    \label{fig:tcommdefqmpimulti}
\end{figure}

Figure \ref{fig:tcompdefqmpimulti} and \ref{fig:tcommdefqmpimulti} illustrate the computation time and communication time for each iteration of SpMV, respectively. We can observe that the computation time decreases as the number of nodes increases, which is expected. As was the case for Figure \ref{fig:tcommdefqmulti} Strategy E performs worse but does exhibit trends in decreasing the communication time as the number of nodes increases. Notably, Strategy C (and Strategy B on some matrices) yield communication times comparable to that of Strategy D, with Strategy C beating D in some matrices. This indicates that in the multi-node setting, communication minimal strategies don't always result in the lowest communication times. 


\begin{figure}[H]
    \centering
    \incfig{commload\_2x4\_multi\_defqmpi}
    \caption{Fraction of the global \(x\) vector communicated per iteration of SpMV on 1–4 nodes (one MPI rank per socket) using dual-socket \defq{} processors.}
    \label{fig:commloaddefqmpimulti}
\end{figure}
