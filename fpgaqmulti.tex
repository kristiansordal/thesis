\subsubsection{One MPI rank per node}
\begin{figure}[H]
    \centering
    \incfig[1]{gflops\_2x4\_multi\_fpgaq}
    \caption{Sustained GFLOPS performance of SpMV over 100 iterations on 1–4 nodes (one MPI rank per node) using dual-socket \fpgaq{} processors.}
    \label{fig:gflopsfpgaqmulti}
\end{figure}

\begin{figure}[H]
    \centering
    \incfig[1]{t\_2x4\_multi\_fpgaq}
    \caption{Computation time per iteration of SpMV on 1–4 nodes (one MPI rank per node) using dual-socket \fpgaq{} processors.}
    \label{fig:tfpgaqmulti}
\end{figure}

\begin{figure}[H]
    \centering
    \incfig[1]{tcomp\_2x4\_multi\_fpgaq}
    \caption{Computation time per iteration of SpMV on 1–4 nodes (one MPI rank per node) using dual-socket \fpgaq{} processors.}
    \label{fig:tcompfpgaqmulti}
\end{figure}

\begin{figure}[H]
    \centering
    \incfig[1]{tcomm\_2x4\_multi\_fpgaq}
    \caption{Communication time per iteration of SpMV on 1–4 nodes (one MPI rank per node) using dual-socket \fpgaq{} processors.}
    \label{fig:tcommfpgaqmulti}
\end{figure}


\begin{figure}[H]
    \centering
    \incfig{commload\_2x4\_multi\_fpgaq}
    \caption{Fraction of the global \(x\) vector communicated per iteration of SpMV on 1–4 nodes (one MPI rank per node) using dual-socket \fpgaq{} processors.}
    \label{fig:commloadfpgaqmulti}
\end{figure}

\subsubsection{One MPI rank per socket}

\begin{figure}[H]
    \centering
    \incfig[1]{gflops\_2x4\_multi\_fpgaqmpi}
    \caption{Sustained GFLOPS performance of SpMV over 100 iterations on 1–4 nodes (one MPI rank per socket) using dual-socket \fpgaq{} processors.}
    \label{fig:gflopsfpgaqmpimulti}
\end{figure}

\begin{figure}[H]
    \centering
    \incfig[1]{t\_2x4\_multi\_fpgaqmpi}
    \caption{Computation time per iteration of SpMV on 1–4 nodes (one MPI rank per socket) using dual-socket \fpgaq{} processors.}
    \label{fig:tfpgaqmpimulti}
\end{figure}

\begin{figure}[H]
    \centering
    \incfig[1]{tcomp\_2x4\_multi\_fpgaqmpi}
    \caption{Computation time per iteration of SpMV on 1–4 nodes (one MPI rank per socket) using dual-socket \fpgaq{} processors.}
    \label{fig:tcompfpgaqmpimulti}
\end{figure}


\begin{figure}[H]
    \centering
    \incfig[1]{tcomm\_2x4\_multi\_fpgaqmpi}
    \caption{Communication time per iteration of SpMV on 1–4 nodes (one MPI rank per socket) using dual-socket \fpgaq{} processors.}
    \label{fig:tcommfpgaqmpimulti}
\end{figure}


\begin{figure}[H]
    \centering
    \incfig{commload\_2x4\_multi\_fpgaqmpi}
    \caption{Fraction of the global \(x\) vector communicated per iteration of SpMV on 1–4 nodes (one MPI rank per socket) using dual-socket \fpgaq{} processors.}
    \label{fig:commloadfpgaqmpimulti}
\end{figure}
